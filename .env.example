#############################################
# Environment Configuration Example
# Copy this file to .env.local (Next.js) or .env and adjust values.
#############################################

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/school_db?schema=public

# JWT secret (change in production!)
JWT_SECRET=change-me-jwt-secret

# AES secret used for encrypting user passwords (must match existing encrypted data)
# If you already migrated passwords with the previous script, keep the same value below.
AES_SECRET_KEY=school-management-secret-key-2025

# NextAuth (if/when used)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-nextauth-secret
